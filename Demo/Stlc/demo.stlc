def id : _ → _ := λ x : ⋆ . x

def alias : _ := id

def twice : _ → _ := λ x : _ . id ( id x )

def applied : _ := ( λ f : ⋆ → ⋆ . f ) id

def nested : _ := ( λ f : ⋆ → ⋆ . f ) ( ( λ g : ⋆ → ⋆ . g ) id )
